name: Build iOS IPA

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.x'
          channel: 'stable'

      - name: Backup Custom Source Code
        run: |
          mkdir -p /tmp/backup_src
          cp -r lib /tmp/backup_src/ || true
          cp pubspec.yaml /tmp/backup_src/ || true

      - name: Generate Missing iOS Project Files
        run: flutter create . --project-name=workshop_copilot --platforms=ios

      - name: Restore Custom Source Code
        run: |
          cp -r /tmp/backup_src/lib/* lib/ || true
          cp /tmp/backup_src/pubspec.yaml . || true

      - name: Configure Microphone & Speech Permissions
        run: |
          PLIST="ios/Runner/Info.plist"
          /usr/libexec/PlistBuddy -c "Add :NSMicrophoneUsageDescription string 'Ứng dụng cần micro để nghe tiếng Trung'" $PLIST || true
          /usr/libexec/PlistBuddy -c "Add :NSSpeechRecognitionUsageDescription string 'Ứng dụng cần nhận diện giọng nói'" $PLIST || true

      - name: Install Dependencies
        run: flutter pub get

      - name: Build iOS
        run: flutter build ios --release --no-codesign

      - name: Compress App to IPA
        run: |
          cd build/ios/iphoneos
          mkdir Payload
          cp -r Runner.app Payload/
          zip -r WorkshopCopilot.ipa Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: WorkshopCopilot-IPA
          path: build/ios/iphoneos/WorkshopCopilot.ipa
